<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.baidu.fuguoyong.easyshop">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- SD卡读写权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".mian.user.login.EasyShopApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".mian.MainActivity" />
        <activity android:name=".mian.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mian.user.LonginActivity" />
        <activity android:name=".mian.user.RegisterActivity" />
        <activity android:name=".mian.user.login.Person.PersonActivity" />
        <activity android:name=".mian.user.login.Person.NicknameActivity" />
        <activity android:name=".mian.shop.details.DetailActivity" />
        <activity android:name=".mian.shop.details.DetailShowActivity" />
        <activity android:name=".mian.me.persongoods.PersongoodActivity" />
        <activity android:name=".mian.shop.details.goodsupdata.GoodsUpLoadActivity" />
        <activity android:name=".mian.shop.details.goodsupdata.GoodsShowActivity"></activity>
    </application>

</manifest>